local library = loadstring(game:HttpGet("https://raw.githubusercontent.com/reaperXKSGIV/ml/refs/heads/main/Eternal%20Hub/Clan%20Script/volcano%20hub%20x%20ui", true))()

local Players = game:GetService("Players")
local HttpService = game:GetService("HttpService")

local localPlayer = Players.LocalPlayer
local username = localPlayer.Name
local userId = localPlayer.UserId

local Player = Players.LocalPlayer
local player = game.Players.LocalPlayer
local VirtualInputManager = game:GetService("VirtualInputManager")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local muscleEvent = player:WaitForChild("muscleEvent")
local leaderstats = player:WaitForChild("leaderstats")
local rebirthsStat = leaderstats:WaitForChild("Rebirths")

local window = library:AddWindow("// Eternal ETRNL  // CLAN VERSION - Welcome " .. localPlayer.DisplayName, {
	title_bar = {Color3.fromRGB(0, 0, 0), Color3.fromRGB(128, 0, 128), Color3.fromRGB(128, 0, 128)}, -- Title Bar Gradient (1-3 Colors)
	title_bar_transparency = 0.2, -- Title Bar transparency (0-1 with 1 being fully transparent)
	background = {Color3.fromRGB(0, 0, 0), Color3.fromRGB(128, 0, 128), Color3.fromRGB(128, 0, 128)}, -- (Background Gradient 1-3 Colors)
	background_transparency = 0.1, -- Background transparency (0-1 with 1 being fully transparent)
	main_color = Color3.fromRGB(0, 0, 0), -- Main Color for some Elements like Particles
	min_size = Vector2.new(600, 600), -- Size of the Window (vertically, horizontally)
	can_resize = true -- resizable (PC exclusive)
})
